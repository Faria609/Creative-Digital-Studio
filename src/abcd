import React from "react";
import { Swiper, SwiperSlide } from "swiper/react";
import "swiper/css"; // Swiper styles
import "swiper/css/navigation";
import "swiper/css/pagination";
import { Navigation, Pagination } from "swiper/modules";
import "./ReviewSection.css";

const CustomerReviews = () => {
  const reviews = [
    {
      text: "Mediadunes did an incredible job under very tight timelines. The re-design of the product received very positive reviews and is now in use by more than 200,000 users. The design is durable, sleek, and was completed in record time. I would highly recommend working with Mediadunes for any project.",
      name: "Brandon Busito",
      title: "CEO, CoVerified",
    },
    {
      text: "Mediadunes exceeded expectations in delivering a high-quality product on time. The team was incredibly responsive and their designs received widespread praise from our users.",
      name: "Sarah Johnson",
      title: "Founder, TechStart",
    },
    {
      text: "Mediadunes exceeded expectations in delivering a high-quality product on time. The team was incredibly responsive and their designs received widespread praise from our users.",
      name: "Sarah Johnson",
      title: "Founder, TechStart",
    },
  ];

  return (
    <section className="reviews-section">
      <h2>Our Customer Reviews</h2>
      <Swiper
  modules={[Navigation, Pagination]}
  spaceBetween={15} // Space between cards
  slidesPerView={1}
  navigation
  pagination={{ clickable: true }}
  breakpoints={{
    640: { slidesPerView: 1 },
    768: { slidesPerView: 2 },
  }}
>
  {reviews.map((review, index) => (
    <SwiperSlide key={index}>
      <div className="review-card">
        <blockquote>
          <p>{review.text}</p>
          <footer>
            <div className="avatar-placeholder"></div>
            <h4>{review.name}</h4>
            <p>{review.title}</p>
          </footer>
        </blockquote>
      </div>
    </SwiperSlide>
  ))}
</Swiper>

    </section>
  );
};

export default CustomerReviews;







.reviews-section {
    text-align: center;
    background-color: #F2E7FE;
    padding: 50px 20px;
  }
  
  .reviews-section h2 {
    font-size: 2.5rem; /* Larger font for section title */
    color: #4D3EE9;
    margin-bottom: 40px;
  }
  
  .review-card {
    width: 100%;
    max-width: 500px; /* Adjust size */
    height: 450px;
    background: #ffffff;
    padding: 30px 20px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    text-align: left;
    margin: auto;
    transition: transform 0.3s, box-shadow 0.3s;
  }
  
  .review-card:hover {
    transform: scale(1.02); /* Add a subtle hover effect */
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
  }
  
  .review-card blockquote {
    margin: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    flex-grow: 1;
    position: relative;
  }
  
  .review-card p {
    text-align: center;
    font-size: 22px;
    font-weight: 700;
    color: #000000A6;
    line-height: 35px;
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    /* -webkit-line-clamp: 7;  */
    /* Show up to 7 lines for long text */
    -webkit-box-orient: vertical;
  }
  
  .review-card footer {
    margin-top: 20px;
    display: flex;
    align-items: center;
    gap: 15px;
  }
  
  .review-card footer .avatar-placeholder {
    width: 50px;
    height: 50px;
    background-color: #ddd;
    border-radius: 50%;
    align-items: center;
  }
  
  .review-card footer h4 {
    text-align: center;
    margin: 0;
    font-size: 1rem;
    font-weight: bold;
    color: #000000A6;
  }
  
  .review-card footer p {
    margin: 0;
    font-size: 0.9rem;
    color: #777777;
  }
  
  /* Swiper-specific styles */
  .swiper-slide {
    display: flex;
    justify-content: center;
  }
  
  .swiper-wrapper {
    gap: 30px; /* Add spacing between Swiper slides */
  }
  
  .swiper-pagination-bullet {
    background: #5a55ca;
  }
  
  .swiper-button-next,
  .swiper-button-prev {
    color: #5a55ca;
  }
  
  /* Responsive Design */
  @media screen and (max-width: 1024px) {
    .reviews-section h2 {
      font-size: 2rem; /* Adjust title font size */
    }
  
    .review-card {
      max-width: 90%; /* Allow cards to shrink */
      height: auto; /* Let height adjust based on content */
      padding: 20px;
    }
  
    .review-card p {
      font-size: 0.95rem;
      /* -webkit-line-clamp: 9;  */
      /* Show more lines for smaller screens */
    }
  }
  
  @media screen and (max-width: 768px) {
    .review-card {
      max-width: 95%; /* Adjust card size */
      padding: 15px;
    }
  
    .review-card p {
      font-size: 0.9rem;
    }
  
    .reviews-section h2 {
      font-size: 1.8rem;
    }
  }
  
  @media screen and (max-width: 480px) {
    .review-card {
      max-width: 100%; /* Full width on very small screens */
      padding: 15px;
      height: auto;
    }
  
    .review-card p {
      font-size: 0.85rem;
      /* -webkit-line-clamp: none; */
       /* Allow full content on small screens */
    }
  
    .review-card footer h4 {
      font-size: 0.9rem; /* Smaller font for small screens */
    }
  
    .review-card footer p {
      font-size: 0.8rem;
    }
  }